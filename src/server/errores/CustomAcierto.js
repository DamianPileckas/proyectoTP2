class CustomAcierto {
    constructor(estado, descripcion, detalles) {
        this.estado = estado;
        this.descripcion = descripcion;
        this.detalles = detalles;
    }
}

export default CustomAcierto;